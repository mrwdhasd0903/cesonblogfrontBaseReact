/**
 * 加一层封装, 可以在此导出常用的组件, 在引入的时候就可以少写一个import
 */

import React, { useState, useEffect,createRef,useRef } from 'react';
import Svg from "@cp/Svg"
export default React

export { useState, useEffect,createRef,useRef, Svg }